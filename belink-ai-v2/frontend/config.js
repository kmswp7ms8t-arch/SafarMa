// Public runtime configuration only. Never put OPENAI_API_KEY or other secrets here.
window.BELINK_AI_CONFIG = Object.freeze({
  // Example: "https://belink-ai-api.example.com"
  // Leave blank for same-origin deployments. On localhost, use http://127.0.0.1:8421.
  apiBase: ""
});
